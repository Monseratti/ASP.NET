@model IEnumerable<MyMarketplase.Models.WarehouseNomenclature>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nomenclature)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NomAmount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NomPrice)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Warehouse)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nomenclature.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NomAmount)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NomPrice)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Warehouse.Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
